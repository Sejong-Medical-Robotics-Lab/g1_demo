# 2026-08-25 작업 정리 — Nav2 실주행 2일차: 회전과의 전쟁

## 오늘 잡은 문제들 (전부 실기체 검증)

| 증상 | 근본 원인 | 처방 |
|---|---|---|
| `map:=` 인자로 지도가 안 바뀜 / 아예 안 뜸 | Jazzy nav2_bringup 이 launch 인자를 params 에 안 꽂음 — `yaml_filename: ""` 이 빈 채로 map_server 도달 | **경로 하드코딩** + 교체는 `load_map` 서비스. `map:=` 인자는 이 환경에서 신뢰 금지 |
| 초기 위치 5연속 거부 | RViz 가 "지금" 스탬프를 찍는데 FAST-LIO TF 는 10Hz(최대 100ms 지연) — AMCL 초기위치 경로는 이 어긋남을 안 기다림 | **set_pose.sh** — Publish Point 클릭 + 각도 인자로 stamp=0 발행 (0 = 최신 TF 사용, 항상 성공) |
| 사람 지나간 자리가 영영 안 지워짐 | 뒤가 뻥 뚫린 방향은 inf 광선 → 기본 설정이 무시 → 그 칸을 통과하는 광선이 없음 | `inf_is_valid: true` (양쪽 costmap) |
| 일직선 추종자의 흔적이 안 지워짐 | 추종자 몸이 자기 옛 흔적을 가림 — 광선이 항상 현재 몸에서 끝나 옛 칸에 못 닿음 (기하학적, 설정으로 불가) | **절차**: 대각선 측면 따라가기 + 목표 직전 `clearmap.sh` |
| "가다가 말음" (진전 없음 중단) | Nav2 의 미세 회전 명령(0.04rad/s)을 로봇이 무시 — 인 줄 알았으나… | ↓ 아래 게걸음 발견으로 진단 교체 |
| **제자리 회전이 게걸음이 됨** (벽 돌진) | **G1 은 vx=0 + 작은 vyaw(≤0.15)를 받으면 돌지 않고 옆으로 미끄러진다** — 사다리 실험으로 0.25 부터 정상 회전 확인 | 브리지 **이중 하한**: 전진 중 회전 ≥0.10 / **제자리 회전 ≥0.25** + 회전 상한 0.3 |
| spin 회복이 벽 돌진 유발 → 뺐더니 BT 로드 실패 | 기본 동작트리가 spin 존재를 전제 | spin 유지 — 브리지 하한 덕에 이제 spin 도 진짜 회전으로 실행됨 |
| 회전 중 스캔·11자 톱니 흔들림 + 마지막 튐 | **보정 지연**: 계산에 쓴 스캔 시점과 적용 시점 사이(0.3~1초) 로봇이 초당 17도씩 돔 → 낡은 답안이 "툭" — 직진에선 6cm 라 무해, 회전에선 8도라 치명 | ① AMCL 이 FAST-LIO 를 믿게 (alpha 0.4→**0.1**) ② beamskip(사람 빔 채점 제외) ③ **회전 중 보정 동결**(update_min_a 6.28) — 방향은 오도메트리 전담, 보정은 걷기 시작 후 25cm 마다 |
| 복귀 시작 시 좌우 씰룩 | 정확히 180도 = 좌우 등거리 칼날 경계 + Oscillation 크리틱이 11도마다 방향 뒤집기 허용 | `oscillation_reset_angle 0.6` — 고른 방향 34도 유지 |
| 하늘색(부풀림)이 복도 절반 차지 | inflation 0.6m + 완만한 감쇠 | **0.45m + cost_scaling 5.0** (사람 하나 점유폭 1.6→1.3m) |
| 간헐적 기동 실패 (응답 유실 timeout) | pkill 잔재가 공유메모리에 쌓임 | active 2회 미확인 시 재실행 1회 → 연속 실패 시 전체 내리고 `/dev/shm/fastrtps*` 청소 |

## 현재 확정 설정 (nav2_g1_localize.yaml 핵심값)

```
지도: yaml_filename 하드코딩 /home/hong/g1_real/maps/lab_2d.yaml
AMCL: alpha1-4 0.1 / update_min_d 0.25 / update_min_a 6.28(회전 동결)
      recovery 0 / beamskip on / sigma_hit 0.1 / max_beams 240 / tolerance 1.0
속도: vx 0.12 / theta 0.3 (브리지 하한: 전진회전 0.10, 제자리회전 0.25, vx 0.05)
costmap: inf_is_valid / inflation 0.45 / scaling 5.0
기타: oscillation_reset_angle 0.6 / movement_time_allowance 40 / source_timeout 2.0
```

## 운영 도구 (신규 3종)

- `nav.sh` — launch 래퍼, 로그 자동 저장(`logs/latest.log`), 최근 20개 유지
- `set_pose.sh <각도>` — TF 타이밍 무관 초기위치 설정 (Publish Point 클릭)
- `clearmap.sh` — 양쪽 costmap 청소 (새 목표 직전·막혔을 때)

## 표준 주행 절차 (확정)

```
lidar → slam(fast_lio) → g1ros(브리지, 옵션 없이) → bash ~/g1_real/nav.sh
→ active 2회 확인 → 초기위치(안 되면 set_pose.sh) → clearmap
→ 직진(반환점은 벽에서 1m+) → clearmap → 복귀(대각선 측면 따라가기)
```

## B안 키트 — 내장 오도메트리 이식 (준비 완료, 미시험)

erasers 팀 g1_ws 분석 결과: 그들의 매끈함 = ①내장 오도메트리(고주파,
라이다 무관) ②Cartographer 연속 정합(200Hz 외삽) ③MPPI/Omni.
우리 결정: 보행층은 검증됐으니 유지, **위치추정층만 단계 교체**.

파일 5종(기존과 병행 가능): `check_odomstate.py`(0단계 관문),
`g1_odom_bridge.py`, `g1_nav2_odomB.launch.py`, `nav2_g1_odomB.yaml`,
`ODOM_B_GUIDE.md`(절차·주의사항). C(Cartographer)는 B 위에 얹는
다음 단계 — pbstream 재매핑 1회 필요.

## PENDING

- A안(회전 동결) 실주행 판정 — 회전 중 톱니가 사라졌는가
- B안 0단계: 펌웨어가 rt/odommodestate 를 쏘는지
- 데드밴드 정밀 실측(0.06→0.08→0.10, 도는 시점) — 여유 시
- 발표: 전/후 지도 사진, 문제-해결 표(위 표 재사용), 백업 영상

---

# 저녁 세션 — DDS 전쟁과 B안 개통, 그리고 첫 자율 왕복

## ★ 오늘의 결승선: 자율 180도 왕복 첫 완주

B안(내장 오도메트리) 첫 판에서 **직진 → 제자리 180도 회전 → 복귀**를
개입 없이 완주. 오후 내내 잡히지 않던 회전 중 흔들림이 "거의 없음"
수준으로 소멸 — 근본 원인(라이다 10Hz 파이프라인의 보정 지연)을
제거한 결과.

## DDS 크래시 규명 — 실험 사다리 전체 기록

buffer overflow 크래시(구독자 생성 즉사)의 정체를 격리 실험으로 추적:

| 실험 | 결과 | 결론 |
|---|---|---|
| 루프백(자가 발행·구독), 0.10.2/0.10.5 | 크래시 | 로봇 무관, 로컬 문제 |
| 11.0.1 공식 조립휠 루프백 | **수신 OK** | 깨끗한 조립휠만 정상 |
| 폴링 방식(콜백 우회) | 크래시(0.10)/OK(11) | 리스너만의 문제 아님 |
| Python 3.11 + 0.10.2 | 크래시 | 3.12 비호환설 기각 |
| **세대 간**(0.10 발행→11 구독) | 무수신 | 11.x 는 로봇 세대와 짝맺기 불가 |
| cp311용 0.10.x 조립휠 | PyPI 에 없음 | 파이썬 경로 막다른 길 |

**최종 판정: 직접 빌드한 ~/cyclonedds/install 의 libddsc 불량** — 이를
참조해 빌드된 모든 0.10.x 바인딩이 공멸. (TTS 오디오 크래시도 같은
뿌리 유력 — 부활 후보로 PENDING)

## 최종 구조 — ROS C-계층 tap + UDP relay

파이썬 DDS 바인딩 전면 폐기. 배포판 조립 CycloneDDS(C 계층)로 로봇을
직접 듣는다 (erasers 실전 방식):

```
[tapenv 터미널(CycloneDDS)]  g1_odom_tap_ros.py ── UDP:17777 ──▶ [평소 터미널(FastDDS)]  g1_odom_relay.py
        로봇 토픽 구독                                                    /odom + TF(odom→base_link)
```

- 셋업: ros_tap_setup.sh (rmw-cyclonedds apt + unitree_ros2 msg 3종
  빌드(~/uni_ros2_ws, rosidl-generator-dds-idl 필요) + cyclonedds_g1.xml)
- 수신버퍼: sysctl rmem_max 26214400 (영구: /etc/sysctl.d/99-dds.conf)
- "Failed to parse type hash" WARN 대량 = 구세대 펌웨어의 무해한 잡음

## ROS_DOMAIN_ID 사건과 통일

이 PC 는 .bashrc 잔재로 **도메인 33** 에서 돌고 있었음 — sdk2py 는
이를 무시하고 0 직결이라 명령은 됐고 "ROS 로 듣기"만 안 됐던 것.
**전 스택 도메인 0 으로 통일** (.bashrc 수정). 부작용: 평소 터미널
목록에 로봇 토픽이 보이지만 **벤더 경계 때문에 직접 echo 는 안 됨**
(발견만 통과, 데이터는 tap 경유 필수 — 직결 실험으로 확정).

## 채택 오도메트리 스펙 (실측)

- 소스: **/state_estimator/odom_pelvis** (nav_msgs/Odometry, child=pelvis)
- **51.5Hz** (편차 4ms) — FAST-LIO 의 5배
- 1m 직진 실측: **x +1.08** (오차 8%, 합격)
- 정지 시 yaw 편류 **분당 ~1도** (IMU 자이로) → 규칙: **오래 세워뒀다
  재개하면 초기 위치 재설정 후 목표**

## B안 표준 기동 절차 (터미널 6개)

| # | 터미널 | 명령 |
|---|---|---|
| 1 | lidar | `ros2 launch livox_ros_driver2 pc2_MID360s_launch.py` (PointCloud2 판) |
| 2 | **tap (특수)** | `source ~/g1_real/tapenv.sh` → `python3 ~/g1_real/g1_odom_tap_ros.py` |
| 3 | relay | `python3 ~/g1_real/g1_odom_relay.py` |
| 4 | 브리지 | `g1ros` → `python3 ~/g1_real/g1_cmdvel_bridge.py --iface $G1_IFACE` |
| 5 | Nav2 | `ros2 launch ~/g1_real/g1_nav2_odomB.launch.py 2>&1 \| tee ~/g1_real/logs/navB_$(date +%H%M%S).log` |
| 6 | 예비 | set_pose.sh / clearmap.sh / 검증용 |

FAST-LIO 는 띄우지 않는다. RViz: 초기위치 → clearmap → 직진(반환점
복도 중앙) → clearmap → 복귀. 추종자는 대각선 측면.

**규칙: tapenv 는 tap 터미널 전용** — 다른 ros2 명령 금지, 다른
터미널에서 tapenv 금지.

## 환경 자동화 (오늘 확정)

- ~/.bashrc: jazzy + ws_livox + uni_ros2_ws 자동 source, DOMAIN=0
- /etc/sysctl.d/99-dds.conf: rmem_max 영구화

## 폴더 정리 (cleanup_20260825.sh)

- archive/dds_experiments/: 규명 실험 11종 (은퇴, 기록 보존)
- archive/old_stack/: 초기 스택 잔재 3종
- logs/ 깃 제외, 루트 지도 사본 동기화

## PENDING (갱신)

1. **백업 영상 녹화** — 데모 시연 준비 때 몰아서 (시연3 실전 경로 왕복)
2. B안 재현성 — 다른 출발점에서 1회 더
3. A안 스택 처분 결정 — B 안정화 확인 후 (당분간 예비로 보관)
4. TTS 부활 시도 — tapenv 세계에서 (/audio_msg, /api/voice 확인됨)
5. unitree_slam 온보드 토픽 조사 (/unitree_slam/high_rate_odometry 등
   — 로봇이 자체 SLAM 가동 중, C안 지름길 후보)
6. 발표: 문제-해결 표(이 문서 2개 표 재사용), 전/후 지도 사진,
   erasers 비교표, TF 역할분담 그림
7. 데드밴드 정밀 실측 (여유 시)

## 깃 커밋 (정리 후)

```bash
bash ~/g1_real/cleanup_20260825.sh
git add -A
git commit -m "B안 개통: 내장 오도메트리(51Hz) 기반 첫 자율 180도 왕복 성공

- DDS 크래시 규명: 로컬 libddsc 빌드 불량 확정 (실험 사다리 6종, 기록은 SESSION)
- 파이썬 바인딩 폐기 → ROS C-계층 tap(CycloneDDS) + UDP relay(FastDDS) 구조
- unitree_ros2 msg 빌드(~/uni_ros2_ws), cyclonedds_g1.xml, rmem_max 영구화
- ROS_DOMAIN_ID 33→0 통일 (.bashrc 잔재 제거)
- 오도메트리: /state_estimator/odom_pelvis 51.5Hz, 1m 실측 +1.08
- 결과: 회전 중 흔들림 소멸, 직진-180도회전-복귀 자율 완주 (프로젝트 최초)
- 정리: 실험 11종 archive/, logs 깃 제외, 오후 AMCL 튜닝 이력 포함
- 신규: pc2_MID360s(라이다 PointCloud2판), odomB launch/yaml, 운영 sh 3종"
git push
```
