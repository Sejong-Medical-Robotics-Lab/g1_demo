# G1 프로젝트 — 팀 설명용 정리

Unitree G1 휴머노이드를 코드로 제어하고, 센서를 붙이고, 자율주행까지 가는
과정에 대한 정리. 처음 보는 사람도 따라올 수 있게 개념부터 적었다.

*마지막 갱신: 2026-08-22 — 2D SLAM 도입, Nav2 진행 중*

---

## 0. 한 줄 요약

> 조이스틱으로만 조작하던 G1 을 **코드로 제어**하고,
> 머리에 달린 **LiDAR 로 3D 지도**를 만들고,
> **목표 지점까지 스스로 걸어간 다음**,
> 최종적으로 **사람을 인식해 반응**하게 하는 것.

---

## 1. 최종 목표와 단계

원래 계획은 5단계였고, 지금은 그것을 넘어 자율주행(Nav2)까지 확장하고 있다.

| 단계 | 내용 | 상태 |
|---|---|---|
| 1 | 댐핑 → 기립 → 간단한 상체 동작 | ✅ |
| 2 | 보행 (전진 / 회전 / 정지) | ✅ |
| 3 | 보행 + 상체 동작 혼합 | 미착수 |
| 4 | 센서(LiDAR) 시각화 | ✅ |
| 확장 | 3D SLAM — 걸으면서 지도 생성 | ✅ |
| 확장 | Nav2 브리지 (`/cmd_vel` → 로봇) | ✅ |
| 확장 | 2D SLAM (slam_toolbox, 루프 클로저) | ✅ |
| 확장 | **Nav2 자율주행 — 목표 지점까지** | ← 지금 여기 |
| 최종 | 사람 인식 → 반응 모션 | 인식 파트 진행 예정 |

**개발 원칙**은 처음부터 하나였다:
*기능을 하나씩 독립적으로 실기체에서 검증한 뒤 통합한다.*
한 번에 다 넣으면 뭐가 문제인지 알 수 없고, 로봇은 실패 비용이 크다.

---

## 2. 시스템 구성 — 전체 그림

```
                        ┌──────────────────────┐
                        │      내 PC (Ubuntu 24.04)     │
                        │   ROS 2 Jazzy + Python venv   │
                        └──────────────────────┘
                                    │ 유선 (192.168.123.51)
                                    │
                        ┌───────────┴───────────┐
                        │        공유기 / 스위치         │
                        └───────────┬───────────┘
              ┌─────────────────────┼─────────────────────┐
              │                     │                     │
      ┌───────┴───────┐   ┌─────────┴────────┐   ┌────────┴───────┐
      │  운동제어 유닛  │   │   Jetson (PC2)   │   │  LiDAR MID-360s │
      │  .161 (제어)   │   │   .164 (개발용)   │   │  .120 (센서)    │
      └───────────────┘   └──────────────────┘   └────────────────┘
```

**포인트**: 로봇 안에 컴퓨터가 두 대 있다.
- **PC1(운동제어)** — Unitree 가 잠가둔 영역. 보행 알고리즘이 여기서 돈다
- **PC2(Jetson)** — 우리가 쓸 수 있는 개발용. 깊이 카메라가 USB 로 붙어 있다

우리는 로봇 밖 노트북에서 **네트워크로 명령을 보낸다.**

---

## 3. 개념 — 알아야 할 것들

### 3-1. DDS — 로봇과 어떻게 대화하나

G1 은 **DDS** 라는 통신 규약을 쓴다. 특징은 **주소를 몰라도 통신이 된다**는 것.

```
내 PC: "rt/lowstate 보내는 애 있어?"   ← 네트워크 전체에 방송(멀티캐스트)
로봇:  "나야, 여기로 보낼게"            ← 응답
```

그래서 우리 코드 어디에도 로봇 IP(`192.168.123.161`)가 없다.
대신 **같은 네트워크 대역에 있어야** 하고, **어느 랜카드로 방송할지** 알려줘야 한다.

> **LiDAR 는 정반대다.** 소리쳐 찾는 게 아니라, 설정 파일에 적힌 IP 로
> 일방적으로 데이터를 쏜다. 그래서 LiDAR 설정에는 **내 PC 의 IP** 를 적어야 한다.
> 이 차이를 몰라서 처음에 헤맸다.

### 3-2. FSM — 로봇의 상태 기계

**FSM(Finite State Machine, 유한 상태 기계)** 은 "로봇이 지금 어떤 모드인가"를
번호로 관리하는 구조다. 핵심은 **아무 상태에서 아무 상태로 못 간다**는 것.

```
0 ZeroTorque  →  1 Damp  →  4 Lock Stand  →  501 레귤러 모드
   (힘 없음)     (감쇠)      (뻣뻣하게 섬)     (보행·팔 동작 가능)
```

왜 이렇게 만드나? **안전** 때문이다. 누워 있는 로봇에게 "걸어"라고 하면
위험하니, 애초에 그 명령을 받을 수 없는 상태로 막아둔다.

**우리가 실측으로 알아낸 것** (문서에 없던 값들):

| FSM | 가능한 것 |
|---|---|
| 4 Lock Stand | 서 있기만 |
| 200 | 보행만. **팔 동작은 거부됨(code 7404)** |
| **501 레귤러** | **보행 + 팔 동작 모두** ← 조이스틱 R1+Y 와 같은 모드 |
| 500 | 이 기체에서는 전이 자체가 안 됨 |

`4` 와 `501` 은 **SDK 에 함수가 아예 없어서** 번호를 직접 보내야 한다.
이걸 찾는 데 하루가 걸렸다.

### 3-3. SLAM — 지도를 만들면서 내 위치를 안다

**SLAM(Simultaneous Localization And Mapping)** — 이름 그대로
*지도 작성*과 *위치 추정*을 동시에 한다. 닭과 달걀 문제를 푸는 셈이다.

> 지도를 만들려면 내 위치를 알아야 하고,
> 내 위치를 알려면 지도가 있어야 한다.

우리가 쓰는 것은 **FAST-LIO** — LiDAR-Inertial Odometry.
LiDAR 포인트와 IMU(관성 센서)를 결합해 **오도메트리를 스스로 만들어낸다.**

**왜 이게 중요한가?**
바퀴 로봇은 바퀴가 몇 바퀴 돌았는지로 이동 거리를 안다(오도메트리).
휴머노이드는 그런 게 없다. G1 의 보행 오도메트리를 ROS 로 빼오려면 별도 작업이
필요한데, **FAST-LIO 는 그것 없이도 센서만으로 위치를 추정한다.**

**왜 카메라가 아니라 LiDAR 인가?**

| | LiDAR (MID-360s) | 깊이 카메라 (D435i) |
|---|---|---|
| 시야 | **360도 전방위** | 전방 87도 |
| 사거리 | 40m | 3~6m |
| 조명 | 무관 | 어두우면 성능 저하 |

**360도가 결정적이다.** 로봇이 회전할 때 이전에 본 것을 계속 봐야 위치 추적이
유지되는데, 전방 87도로는 조금만 돌아도 시야가 완전히 바뀐다.
카메라의 자리는 따로 있다 — 물체 인식, 사람 추적. *지도 만들기*가 아니라
*지도 위에서 무엇을 볼 것인가*에 쓴다.

### 3-4. TF — 좌표계를 잇는 나무

로봇에는 좌표계가 여러 개다. LiDAR 기준, 로봇 몸통 기준, 지도 기준…
**TF** 는 이 좌표계들의 관계를 관리한다.

```
map  ──>  odom  ──>  base_link  ──>  (센서들)
지도      오도메트리     로봇 몸통
```

"LiDAR 가 본 점이 지도상 어디인가"를 계산하려면 이 사슬이 이어져 있어야 한다.
Nav2 는 이 구조를 전제로 동작한다.

### 3-5. Nav2 — 자율주행

**Nav2(Navigation2)** 는 ROS 2 의 자율주행 프레임워크다. 하는 일:

```
목표 지점 입력
    ↓
전역 계획 (Planner)      — 지도를 보고 대략의 경로를 그린다
    ↓
지역 계획 (Controller)   — 눈앞의 장애물을 피하며 속도를 낸다
    ↓
/cmd_vel  (속도 명령)
```

**여기서 중요한 점**: Nav2 는 `/cmd_vel` 이라는 토픽으로 "앞으로 0.2m/s,
회전 0.1rad/s" 같은 명령을 내보낼 뿐이다. **로봇을 직접 움직이지 못한다.**
그 명령을 받아 Unitree SDK 로 옮기는 **브리지**가 필요하다. 이게 우리가 만든 것.

---

## 4. 지금까지의 흐름 — 무엇을 어떤 순서로 했나

### Day 1 — 환경과 1단계

**문제 1: 로봇에 연결이 안 됐다.**
SDK 를 초기화하면 프로그램이 통째로 죽었다(`buffer overflow detected`).

원인이 두 개 겹쳐 있었다.
- ROS 2 와 Unitree SDK 가 **서로 다른 버전의 DDS 라이브러리**를 요구 →
  한 터미널에서 둘 다 쓰면 충돌
- SDK 에 랜카드 이름을 넘기면 내부 로그 처리에서 죽음 →
  환경변수(`CYCLONEDDS_URI`)로 대신 지정해 우회

**교훈**: *터미널을 용도별로 분리한다.* 지금도 제어용/센서용을 따로 쓴다.

**문제 2: 기립이 안 됐다.**
SDK 문서대로 `Squat2StandUp()` 을 불렀는데 로봇이 반응하지 않았다.
그런데 **함수는 에러도 안 냈다.**

파고 보니 SDK 의 래퍼 함수들이 **반환값을 버리고 있었다.**
`Damp()` 는 내부적으로 `SetFsmId(1)` 을 부르지만 그 결과 코드를 돌려주지 않는다.
그래서 로봇이 명령을 거부해도 우리 코드는 성공한 줄 알았다.

→ 래퍼 대신 `SetFsmId()` 를 직접 호출해 **반환 코드를 검사**하도록 바꿨다.

그다음 FSM 번호를 하나씩 찔러보는 도구(`g1_fsm_probe.py`)를 만들어
`1 → 4 → 501` 사슬을 실측으로 찾아냈다.

**문제 3: 팔 동작이 거부됐다(code 7404).**
`GetActionList` 응답을 뜯어보니 액션마다 **실행 가능한 FSM 조건**이 붙어 있었다.
FSM 200 에서는 보행만 되고 팔은 안 된다. **501 이어야 둘 다 된다.**

**Day 1 결과**: 댐핑 → 기립 → 레귤러 모드 → 상체 동작까지 코드로 실행 성공.

### Day 2 — 센서와 SLAM

**문제 4: LiDAR 데이터가 안 왔다.**
드라이버는 `Init lds lidar success!` 를 찍는데 그 이후 아무 일도 없었다.
에러 메시지가 **하나도 없었다.** 이게 제일 고약했다.

세 시간 동안 확인한 것들:
- 네트워크 — 정상(ping 1ms)
- 방화벽 — 꺼져 있음
- 프로세스 충돌 — 정리해도 그대로
- 다른 장비가 LiDAR 를 점유 — 아님
- SDK 버전 — 되돌려봤지만 빌드조차 실패

패킷을 직접 뜯어보니 **양쪽이 서로에게 "나 여기 있어"만 반복**하고
그다음 단계로 못 넘어가고 있었다.

**진짜 원인**: 이 기체의 LiDAR 는 일반 MID-360 이 아니라 **MID-360s(신형)** 였다.
설정 파일 형식이 달라서, 일반 MID-360 설정으로는 드라이버가 LiDAR 를
인식하지 못했다. 드라이버 폴더에 **이미 `MID360s_config.json` 이 들어 있었다.**

**교훈**: *원인을 추측해 뭔가 바꾸기 전에, 어떤 선택지가 준비돼 있는지 먼저 본다.*
`ls config/` 한 번이면 끝날 일이었다.

**그다음 3D SLAM(FAST-LIO)**:
- ROS 2 Jazzy 에서 빌드가 안 됨 → 저장소가 C++14 기준이라 C++17 로 올려서 해결
- 입력이 일반 PointCloud2 가 아니라 **CustomMsg** 여야 함
  (포인트마다 타임스탬프가 있어야 모션 왜곡 보정이 가능하기 때문)
- **맵의 방향이 뒤집혀 나옴** — LiDAR 가 로봇 머리에 **거꾸로** 달려 있어서.
  보정 위치를 잘못 짚어 한참 헤맸다. 결론은 드라이버 설정 한 곳에서만
  보정하고 SLAM 쪽은 건드리지 않는 것

**방향 확인법**: 로봇 팔을 위로 흔들어 본다. 지도에서도 위로 그려지면 맞다.
화면의 좌우로 판단하면 안 된다 — 카메라를 앞에서 보면 거울처럼 뒤집혀 보인다.

**Day 2 결과**: 3D 지도 생성 및 저장 성공.

### Day 3 — 보행

**보행 검증.** 안전을 위해 **데드맨(dead-man) 방식**으로 만들었다.

```
속도 명령의 유효 시간을 0.5초로 짧게 주고,
0.2초마다 다시 보낸다.
→ 프로그램이 죽거나 통신이 끊기면 로봇이 0.5초 안에 스스로 멈춘다.
```

SDK 의 `Move()` 함수는 반대다 — 한 번 부르면 **10일치** 명령이 나간다.
스크립트가 죽어도 로봇이 계속 걷는다. 그래서 쓰지 않는다.

걸으면서 SLAM 도 확인했다. 지도는 만들어지지만 **보행 진동 때문에 포인트가
많이 깨진다.** 휴머노이드는 발 딛을 때마다 상하로 흔들리고, LiDAR 가 머리에
있어서 그 진동을 그대로 받는다.

**Day 3 결과**: 전진·회전·정지가 코드로 되고, 걸으면서 지도도 쌓인다.

---

### Day 4 — Nav2 브리지

**Nav2 를 붙이려면 넘어야 할 벽이 하나 있었다.**

Nav2 는 ROS 2 위에서 돌고, 로봇 제어는 Unitree SDK 로 한다.
그런데 이 둘은 Day 1 에 충돌해서 프로그램이 죽었던 조합이다
(서로 다른 버전의 DDS 라이브러리를 요구해서).

그래서 지금까지는 **터미널을 분리**해서 피해 왔다. 하지만 브리지는
"ROS 에서 받아 SDK 로 넘기는" 일이라 **한 프로그램 안에 둘 다 있어야 한다.**

해결책은 ROS 2 쪽이 쓰는 DDS 구현을 바꾸는 것이었다.

```bash
export RMW_IMPLEMENTATION=rmw_fastrtps_cpp
```

ROS 2 가 CycloneDDS 대신 FastDDS 를 쓰게 하면, 프로그램 안에
CycloneDDS 가 하나만(SDK 것만) 로드된다. 충돌이 사라진다.

**만든 것**: `g1_cmdvel_bridge.py`

```
Nav2 ── /cmd_vel ──> [브리지] ──> SetVelocity() ──> G1
```

여기에도 데드맨을 넣었다. `/cmd_vel` 이 0.5초 이상 안 오면 자동으로 정지한다.
**Nav2 가 죽든, 목표에 도달해 명령을 멈추든 로봇은 선다.**
그리고 브리지에서 속도 상한을 걸어두었기 때문에, **Nav2 설정이 잘못돼도
로봇이 튀지 않는다.**

`ros2 topic pub` 으로 직접 속도를 쏴서 로봇이 걷는 것까지 확인했다.
Nav2 는 이 `/cmd_vel` 을 대신 내보내는 것뿐이다.

---

### Day 5 — 2D SLAM 으로 방향 전환

Nav2 를 붙여 목표를 줬더니 **로봇이 엉뚱한 방향으로 갔다.**

원인은 위치 추정이었다. 주행 중에 SLAM 으로 지도와 위치를 **동시에** 추정하면
보행 진동 때문에 둘 다 흔들린다. Nav2 입장에서는 목표가 계속 움직이는 셈이다.

그리고 FAST-LIO 에는 **루프 클로저가 없다** — 한 번 어긋난 위치를 되돌릴
방법이 없어서 오차가 그대로 쌓인다.

**구조를 두 단계로 나눴다.**

```
1단계) 지도를 만들어 저장한다     — slam_toolbox (루프 클로저 O)
2단계) 그 지도를 고정하고 주행한다 — AMCL 이 위치만 찾음 (오차 누적 X)
```

`slam_toolbox` 를 얹은 이유는 **루프 클로저**다. 한 바퀴 돌아 출발점으로
오면 쌓인 오차가 한 번에 보정된다. 덤으로 2D 투영이라 상하 진동이
위치 추정에 개입하지 못하고, 결과물이 Nav2 가 그대로 쓰는 점유격자다.

역할 분담이 명확해졌다.

| | 역할 |
|---|---|
| FAST-LIO | 오도메트리 (단기 정확) |
| slam_toolbox | 지도 + 루프 클로저 (장기 보정) |
| AMCL | 주행 시 고정 지도에 맞춰 위치 확정 |

**여기서도 "조용한 실패"를 만났다.** `slam_toolbox` 는 라이프사이클 노드라
프로세스가 떠도 activate 하지 않으면 아무 일도 하지 않는다. 에러도 안 난다.
RViz 에 "Frame [map] does not exist" 만 뜰 뿐이었다.

---

## 5. 지금 만들어져 있는 것

### 스크립트

| 파일 | 역할 |
|---|---|
| `g1_stand_test.py` | 1단계 — 기립까지 (매 단계 확인받고 진행) |
| `g1_walk_test.py` | 2단계 — 보행 (데드맨 방식) |
| `g1_real_sequence.py` | 주 실행 파일 — 전이 + 상체 + 보행 |
| `g1_real_monitor.py` | 감시 — 온도·토크·기울기 실시간 확인 |
| `g1_cmdvel_bridge.py` | **Nav2 ↔ 로봇 브리지** (검증 완료) |
| `g1_mapping_2d.launch.py` | **2D 지도 작성** (slam_toolbox) |
| `g1_nav2_localize.launch.py` | **자율주행** (고정 지도 + AMCL + Nav2) |
| `nav2_g1_localize.yaml` | Nav2 + AMCL 파라미터 |
| `g1_nav2.launch.py` | (구) 지도 없이 주행 — 참고용 |
| `g1_fsm_probe.py` | FSM 번호를 하나씩 찔러보는 진단 도구 |
| `g1_arm_probe.py` | 팔 동작 진단·순회 |
| `g1_audio_test.py` | 스피커·LED 확인 |
| `g1_imu_view.py` | IMU 실시간 확인 |

### 문서

| 파일 | 내용 |
|---|---|
| `README.md` | 파일 구성, 실행법 |
| `PROGRESS.md` | 진행 상황, 확정값, 다음 할 일 |
| `SETUP.md` | 환경 구축 절차 (새 PC 에서 재현용) |
| `SDK_API.md` | SDK 레퍼런스 |
| `OVERVIEW.md` | 이 문서 — 전체 개념과 흐름 |
| `TASK_perception.md` | 인식 파트 작업 계획 (팀원용) |
| `TASK_camera.md` | 카메라 연결 작업 계획 (팀원용) |
| `NAV2_GUIDE.md` | Nav2 실행 순서 (지도 작성 → 주행) |

저장소: `Sejong-Medical-Robotics-Lab/g1_demo`

---

## 6. 안전 설계 — 이 프로젝트에서 신경 쓴 것

로봇은 실패 비용이 크다. 코드 전반에 다음을 넣었다.

**① 단계마다 사람이 확인한다**
자동으로 쭉 진행하지 않는다. `[게이트] 행어 거치 확인 완료?` 처럼
매 단계 `y` 를 받아야 넘어간다.

**② 명령이 끊기면 로봇이 멈춘다 (데드맨)**
보행 중 프로그램이 죽거나 랜선이 빠져도 0.5초 안에 정지한다.

**③ 속도에 상한을 건다**
브리지에서 clamp 하므로 Nav2 설정이 잘못돼도 로봇이 튀지 않는다.

**④ 실패해도 Damp 를 보내지 않는다**
Damp 는 힘을 빼는 명령이다. **서 있는 로봇에 보내면 주저앉는다.**
오류가 나면 "정지 + 자세 유지" 로 끝내고, 판단은 사람에게 넘긴다.

**⑤ 반환 코드를 믿지 않는다**
`code=0` 이 성공을 뜻하지 않는다는 걸 겪었다.
상태를 다시 읽어 확인하고, **최종 판정은 육안**이다.

---

## 7. 남은 것

| 항목 | 담당 | 비고 |
|---|---|---|
| **Nav2 실기체 테스트** | 자율주행 | 브리지·설정 준비 완료, 실행만 남음 |
| **사람 인식 노드** | 인식 | 웹캠으로 개발 중 (로봇 불필요) |
| 반응 모션 | 자율주행 | 인식 결과를 받아 팔 동작 실행 |
| 3단계 보행 + 상체 | 자율주행 | 시나리오 구성 필요 |
| 깊이 카메라 | 인식 | **Jetson USB 에 카메라가 안 잡힌다** — 케이블 확인 필요 |
| 루프 클로저 | — | FAST-LIO 는 한 바퀴 돌아와도 오차를 보정하지 못함 |

### 파트 나누기와 접점

로봇이 한 대뿐이라 두 갈래로 나눠 진행한다.

```
    [자율주행 파트]                    [인식 파트]
    Nav2 + 모션 실행                   웹캠 사람 인식
    로봇 많이 사용                      로봇 불필요
            │                              │
            └──────  /person_detected  ─────┘
                     (접점은 이 토픽 하나)
```

토픽 형식만 미리 정해두면 서로의 코드를 볼 필요가 없고,
각자 가짜 값으로 테스트할 수 있다. 자세한 내용은 `TASK_perception.md`.

---

## 8. 이 프로젝트에서 배운 것

**공식 문서에 없는 값이 많다.**
FSM 501, MID-360s 구분법, 팔 동작의 FSM 조건 — 전부 실측으로 알아냈다.
그래서 **알아낸 즉시 저장소에 기록**했다. 다시 찾으려면 며칠이 걸린다.

**에러가 안 나는 게 제일 어렵다.**
LiDAR 문제도, 기립 실패도 에러 메시지가 없었다.
"성공한 것처럼 보이는데 아무 일도 안 일어나는" 상황을 다루려면
**중간 단계를 눈으로 확인할 수 있는 도구**가 필요하다.
그래서 진단용 스크립트(`*_probe.py`)를 따로 만들었다.

**추측보다 관찰이 빠르다.**
LiDAR 에서 세 시간을 쓴 건 원인을 추측해 계속 뭔가를 바꿨기 때문이다.
"어떤 파일이 준비돼 있나"를 먼저 봤으면 10분이면 끝났다.
