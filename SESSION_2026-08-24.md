# 2026-08-24 작업 정리 — 지도 청소 · 마운트 · TTS · Nav2 실주행 첫날

## ★ 내일 아침 시작 절차 (이것부터)

**다운로드 폴더에서 두 파일을 덮어쓰면 오늘 밤 수정이 전부 적용된다:**

```bash
cp ~/다운로드/g1_cmdvel_bridge.py  ~/g1_real/
cp ~/다운로드/nav2_g1_localize.yaml ~/g1_real/
cp ~/다운로드/nav.sh               ~/g1_real/ && chmod +x ~/g1_real/nav.sh
```

그 다음 평소처럼:

```bash
# 터미널1: 라이다+FAST-LIO (기존 slam 별칭 절차)
# 터미널2:
bash ~/g1_real/nav.sh          # 인자 없이! 지도는 yaml에 박혀 있고, 로그가 자동 저장됨
# 터미널3:
python3 ~/g1_real/g1_cmdvel_bridge.py --iface $G1_IFACE     # 하한선 기본 켜짐
```

**문제 생기면 `~/g1_real/logs/latest.log` 하나만 올리면 된다** — 화면과
동일한 내용이 통째로 저장된다 (실행마다 새 파일, 최근 20개 자동 유지).

RViz 에서 2D Pose Estimate → 목표(흰 영역 안쪽만!) → 주행.

**덮어쓰는 두 파일에 들어있는 것:**
- `nav2_g1_localize.yaml`: 지도 경로 하드코딩(lab_2d.yaml), AMCL 점프 억제
  (update_min 0.4 / recovery 0), inf_is_valid(유령 장애물 해소),
  회전 상한 0.15 복원, 진행 감시 여유 40초
- `g1_cmdvel_bridge.py`: **실행 하한선 기본 켜짐** (min-vyaw 0.10 /
  min-vx 0.05) — "가다가 말던" 데드밴드 문제의 처방. 트림 옵션도 포함

**여유 있을 때 하면 좋은 실측 (2분):** 데드밴드 정확한 경계 —
```bash
ros2 topic pub -r 10 /cmd_vel geometry_msgs/msg/Twist "{angular: {z: 0.06}}"
# 0.06→0.08→0.10 올려가며 처음 도는 값 확인. 0.10 과 크게 다르면
# --min-vyaw 로 그 값을 넘겨서 실행 (기본값보다 정밀)
```


## 오늘 끝낸 것

### ① 2D 지도 청소 (lab_2d 계열)
- 1번 구역(사각형 왼쪽 아래 벽 끊김): 실측 벽 통계(흔들림 σ, 잔점 밀도)를
  반영한 자연스러운 선으로 연결. 사용자가 GIMP 으로 직접 다듬은 버전을
  검증(크기 일치, 228px 변경 확인)하고 안티앨리어싱 잔값 35px 을 표준값
  {0,205,254} 로 스냅 → `maps/lab_2d_step2_snapped.pgm`
- 2번 구역(오른쪽 통유리 너머 부채꼴 누출): "연결성" 기준은 실패 —
  진짜 건물은 복도로 다 이어져 있어 누출과 정상 복도를 구분 못 한다.
  **위치 기반**(벽 뒤 좁은 범위 무조건 미지)으로 해결
  → `maps/lab_2d_step3.pgm`
- 파일이 뷰어에서 안 열리는 문제: 형식은 완전 정상(P5, 3값) —
  GNOME 썸네일 캐시 문제였음. `rm -rf ~/.cache/thumbnails`

### ② 머리 카메라 마운트 (D435i, 3D 프린팅)
- 공식 head_link.STL 과 대조해 **정수리 곡률 75→68.9mm 정정**
  (최소자승 구 피팅, 잔차 평균 1.0mm). 안장-실물 간격 평균 0.7mm 검증
- LiDAR 는 돔 아래 빈 공간 천장에 거꾸로 매달려 옆을 봄(실물 확인,
  deepglint 저장소의 roll=180 설정으로도 교차 확인) → 시야 제약 해제
- 안장 반지름 33→**45mm 확장** (55mm 는 실측 대비 최대 8mm 떠서 기각)
- 슬롯 z 좌표가 고정값이라 반지름 키우면 허공을 자르는 버그 발견·수정
  (구 방정식으로 동적 계산)
- 고정 방식: 벨크로 스트랩 십자 (접착 버전은 만들지 않기로 결정)
- 산출물: `g1_head_cam_mount.scad`, `mount_stl/g1_head_cam_mount.stl`,
  `verify/check_fit.scad`(+head_link.STL — 실물 메시와 겹쳐보기)

### ③ 오프라인 한국어 TTS (`g1_speak.py`)
- 배경: 로봇 내장 TtsMaker 는 중국어만 됨. 한국어·영어는 무음.
  조이스틱의 영어는 펌웨어에 미리 녹음된 파일 재생으로 추정
- 해법: PC 에서 합성 → `AudioClient.PlayStream()` 으로 파형 스트리밍
  (SDK 공식 예제 방식, 16kHz/모노/16bit, 3초 청크)
- 엔진 2종: espeak-ng(apt 설치만으로 완전 오프라인, 기계적 음질) /
  piper(`--engine piper`, 자연스러움, 모델 1회 다운로드 필요)
- **정확한 한국어 모델명: `ko_KR-kss-medium`** (hana 는 404 — 목록은
  `python3 -m piper.download_voices` 인자 없이 실행)
- 합성까지 전부 검증 완료. 로봇 재생 단계에서 buffer overflow 크래시 —
  모션·FSM·자세인식은 전부 정상이라 오디오 클라이언트에 국한된 문제.
  **사용자 결정: 오디오는 포기. 재개 시 "합성은 되고 전송만 죽는 지점"부터**

### ④ g1_pose_action.py 로그 정리
- mediapipe/TFLite C++ 로그: 환경변수(GLOG_minloglevel 등) + fd 레벨
  stderr 스왑(quiet_native_stderr — C++ 가 fd 2 에 직접 쓰는 건 Python
  재지정으로 못 잡음, os.dup2 필요) 이중 억제
- 동작 실행 줄만 굵은 초록(▶), 경고 노랑, isatty 체크로 리다이렉트 시 해제
- 부작용 복구: 오디오 디버깅 중 opencv 제거 → pose_action 깨짐 → 재설치
  (numpy 2.5.2 동반, mediapipe 의존성 경고는 무시 가능 — 실전 정상 확인)

### ⑤ G1 SLAM 커뮤니티 조사
- deepglint/FAST_LIO_LOCALIZATION_HUMANOID (별 820): FAST-LIO2 오도메트리
  + Open3D 3D 정합 위치추정. 오프라인 지도 + 런타임 위치추정 — 우리와
  같은 철학. CloudCompare 로 지도 청소(사람 그림자 제거) 권장 = 우리가
  한 작업이 표준 워크플로우. ROS1 + Humble 브랜치(2025-12), **Jazzy 없음**
- 우리 스택은 이미 Jazzy 네이티브로 동작 중. AMCL 이 실제로 문제일 때만
  Docker Humble 컨테이너로 deepglint 를 검토하면 됨
- Unitree 공식 SLAM 은 확장 도크 로봇독 전용 — G1 용 없음

### ⑥ Nav2 실주행 1일차 (밤) — 최종 결산
- **지도 안 뜨던 근본 원인 확정**: params 의 `yaml_filename: ""` + Jazzy
  nav2_bringup 이 `map:=` 인자를 params 에 안 꽂아줌 → 빈 문자열 도달.
  **경로 하드코딩으로 종결** (지도 교체는 load_map 서비스 사용)
- 유령 장애물(정면 하늘색): 사용자가 머리 pitch 물리 조정으로 해소
- **사람이 지나간 자리가 안 지워지는 문제**: `inf_is_valid` 누락이 원인 —
  사람 뒤가 뻥 뚫린 방향이면 inf 광선이 무시돼 영영 안 지워짐.
  양쪽 costmap 에 `inf_is_valid: true` 추가로 해결 (적용 확인됨)
- AMCL 점프: update_min 0.4 / recovery_alpha 0 → **위치 추정 안정 확인**
- **"가다가 말던" 문제 — 데드밴드 확정**: 브리지가 vyaw=+0.04 를 보내는
  동안 로봇이 미동도 없음을 사용자가 눈으로 확인. 회전 상한을 0.08 로
  낮춘 것이 부작용의 근원(보정 명령 대부분이 데드밴드 아래로 깔림).
  처방: ① 회전 상한 0.15 복원 ② 브리지 실행 하한선(기본 0.10/0.05)
  ③ 진행 감시 여유 20→40초 (0.15rad/s 로 180도 회전에 21초)
- 우측 벽 드리프트도 같은 데드밴드가 원인으로 수렴 — 보정 회전이 씹혀서.
  내일 하한선 켠 상태로 재관찰, 남으면 --trim-vyaw 로 상쇄
- 기타 교훈: 목표는 흰 자유공간 안쪽만(회색 찍으면 "Failed to create
  plan"), 주행 중 2D Pose Estimate 재클릭 금지(취소→재설정→새 목표 순서),
  노트북 배터리 절전이 action server 타임아웃 유발(전원 연결 필수)

---

## 깃허브 최신화

`~/g1_real` 저장소 기준. 다운로드 폴더의 새 파일을 덮어쓴 뒤:

```bash
cd ~/g1_real

# ── 오늘 갱신·추가된 파일 (다운로드에서 복사) ─────────────────────
#  g1_speak.py               (신규 — 오프라인 TTS)
#  g1_pose_action.py         (갱신 — 로그 억제 + 색상 출력)
#  g1_cmdvel_bridge.py       (갱신 — 트림 기능 추가)
#  nav2_g1_localize.yaml     (갱신 — AMCL 점프 억제 + 속도 상한)
#  g1_head_cam_mount.scad    (갱신 — 68.9mm/45mm 실측 반영)
#  mount_stl/g1_head_cam_mount.stl
#  verify/check_fit.scad, verify/head_link.STL   (신규 폴더)
#  MOUNT_GUIDE.md            (갱신)
#  SESSION_2026-08-24.md     (신규 — 이 파일)
#  maps/lab_2d_step2_snapped.{pgm,yaml}
#  maps/lab_2d_step3.{pgm,yaml}
#  maps/lab_2d_1.{pgm,yaml}  (오늘 실주행에 쓴 지도)

# ── 지난번부터 걸려 있던 것: lab.pcd 추적 해제 (대용량) ───────────
git rm --cached maps/lab.pcd
echo "maps/*.pcd" >> .gitignore

git add .
git commit -m "지도청소·마운트45mm·오프라인TTS·Nav2 실주행 튜닝(데드밴드 처방)

- 지도: 1구역 연결+스냅, 유리벽 미지화(step3), 최종 실주행 지도는 lab_2d
- map:= 인자 미전달(Jazzy nav2_bringup) 확정 → yaml_filename 하드코딩
- costmap inf_is_valid 추가 — 사람 지나간 자리 유령 잔존 해결
- AMCL: update_min 0.4 / recovery 0 (점프 해결, 실기체 검증)
- 데드밴드 확정(vyaw 0.04 무시 목격) → 회전상한 0.15 복원 +
  브리지 실행하한선 기본화(0.10/0.05) + 진행감시 40초
- 마운트: 곡률 68.9mm 실측 정정, 안장 45mm, 슬롯 z 동적계산 버그 수정
- TTS: g1_speak.py (espeak-ng/piper) — 합성 검증, 로봇 재생은 보류
- pose_action: 네이티브 로그 억제 + 색상 출력"

# push 는 토큰 인증 필요 (지난번과 동일)
git push
```

**주의**: `nav2_g1_localize.yaml` 저장소 사본에는 속도 상한(0.12/0.08)도
반영해뒀다. **저녁 전에 sed 로 적용하셨다는 가정**이니, 실기체 파일과
다르면 실기체 쪽 값을 기준으로 맞출 것.

---

## 우측 드리프트 — 심층 분석

### 확정된 사실
1. 경로 생성 정상 (복도 가운데로 그려짐)
2. 위치 추정 정상 (AMCL 튜닝 후 "훨씬 좋아짐")
3. 로봇이 **실행 중** 오른쪽 벽으로 붙음
4. **빨간 경로선이 로봇의 (틀어진) 위치를 따라 다시 그려짐** ← 결정적 관찰
5. `nav2_g1_localize.yaml` 에서 `max_vel_y: 0.0`, `vy_samples: 1`
   → **Nav2 는 횡이동(vy)을 아예 명령하지 않는다.** 좌우 보정 수단은
   회전(vyaw)뿐 (vy 부호 반전 가설은 이걸로 기각)

### 그림: 세 가지가 겹친 것으로 추정

**(A) 근원 — 보행 자체가 오른쪽으로 샌다.**
다리 로봇이 저속(0.12m/s)에서 좌우 걸음 비대칭으로 한쪽으로 새는 건
흔한 특성이다. 여기에 라이다 장착 yaw 오차(오도메트리 기준 "정면"과
기계적 정면의 어긋남) 가능성도 겹친다 — yaw 3° 오차면 1m 전진마다
5cm 씩 옆으로 샌다. 오늘 머리 각도를 만진 것도 관련 있을 수 있다.

**(B) 증폭 — 보정 명령이 로봇의 데드밴드에 먹힌다.**
저녁 전에 AMCL 안정화를 위해 `max_vel_theta` 를 0.08 로 낮췄다.
그런데 G1 보행 컨트롤러가 아주 작은 vyaw(예: 0.05 미만)를 무시하는
데드밴드가 있다면, Nav2 가 내는 소량의 좌회전 보정(0.02~0.06)이
**종이 위에선 나가지만 로봇은 안 도는** 상태가 된다. 보정 수단이
vyaw 뿐인데 그 vyaw 가 실행이 안 되면 드리프트는 무저항이다.

**(C) 은폐 — 재계획이 오차를 매번 용서한다.**
Nav2 기본 동작트리는 약 1초마다 **현재 위치에서** 전역 경로를 다시
계산한다. 로봇이 오른쪽으로 새면, 경로가 그 새 위치에서 다시 그려져
횡오차가 리셋된다 — "빨간선이 로봇을 따라 이동"하는 관찰이 정확히
이것이다. PathAlign/PathDist 크리틱이 싸울 오차 자체가 매초 지워지니,
컨트롤러는 아무 문제 없다고 판단하며 로봇은 부드러운 호를 그리며
벽으로 간다.

### 저녁 후 진단 순서 (각 1~2분)

**진단 1 — 컨트롤러가 보정을 시도는 하는가**
드리프트가 일어나는 동안:
```bash
ros2 topic echo /cmd_vel --field angular.z
```
- 작은 양수(+0.02~0.06)가 계속 나오는데 로봇이 안 돈다 → **(B) 데드밴드 확정**
- 거의 0 이다 → (C) 가 지배적, 컨트롤러가 오차를 못 보고 있음

**진단 2 — Nav2 없이 순수 직진 기준선**
Nav2 는 끄고 브리지만 둔 채:
```bash
ros2 topic pub -r 10 /cmd_vel geometry_msgs/msg/Twist "{linear: {x: 0.12}}"
```
5m 걷게 하고 옆으로 얼마나 새는지 잰다 → **(A) 의 크기 확정.**
(안전: 행어 없이 하는 거면 좁은 복도 말고 넓은 곳에서, 리모컨 대기)

**진단 3 — 트림 값 찾기**
같은 직진에 회전 편향을 조금씩 섞어 직진이 되는 값을 찾는다:
```bash
ros2 topic pub -r 10 /cmd_vel geometry_msgs/msg/Twist \
    "{linear: {x: 0.12}, angular: {z: 0.02}}"   # 0.02→0.04→... (+ = 좌회전)
```

### 조치 (진단 결과에 따라)

**즉효약 — 브리지 트림 (오늘 넣어둠):**
```bash
python3 g1_cmdvel_bridge.py --iface $G1_IFACE --trim-vyaw 0.03
```
진단 3 에서 찾은 값을 넣으면 전진 중 항상 그만큼 좌회전이 더해져
기계적 드리프트를 상쇄한다. 정지·데드맨 상태에는 절대 안 더해진다.

**(B) 가 확인되면 — max_vel_theta 0.08 → 0.15 복원 검토:**
AMCL 점프는 이제 update_min/recovery 로 잡았으니, 회전 상한을
데드밴드 위로 되돌려도 위치 추정이 버틸 가능성이 높다. 보정 명령이
실제로 실행되게 하는 근본 처방이다.

**보조 — 벽 자체를 더 비싸게:**
`inflation_radius 0.6 → 0.8` 로 올리면 벽에 붙는 경로의 비용이 커져
컨트롤러가 중앙 쪽 궤적을 선호하게 된다. 복도 폭이 충분할 때만.

### 왜 "명령 문제 같다"는 직감이 절반은 맞는가
명령이 잘못 나가는 건 아니지만(**vy 는 아예 0, vyaw 는 정상 부호**),
**나간 명령이 로봇에서 실행이 안 되는 것(B)** 이거나 **필요한 명령이
생성되지 않는 것(C)** 일 가능성이 높다 — 둘 다 넓은 의미의 "명령
계층" 문제가 맞다. 진단 1 이 둘을 가른다.
